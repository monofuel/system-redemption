version: '3.2'
services:
  standalone:
    image: ${CONTAINER_REGISTRY}/sr_main:latest
    build:
      # NB. this does not actually work
      # https://github.com/docker/compose/issues/883
      cache_from:
        -  ${CONTAINER_REGISTRY}/sr_main:builder
        -  ${CONTAINER_REGISTRY}/sr_main:latest
      context: .
    restart: always
    ports:
      - '3000:3000'
    environment:
      SR_INTERFACE: 0.0.0.0
